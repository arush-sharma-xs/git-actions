name: Test- Manual trigger
run-name: "Test to deploy manual trigger"
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: echo "Test"
